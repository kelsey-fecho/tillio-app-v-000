<h1><%= @user.name %>'s Clippings</h1>

<%= link_to "Browse Plants", plants_path %><br><br>
<hr>

<% @clippings.each do |c|%>
  <%= link_to Plant.find(c.plant_id).name, plant_path(c.plant) %>
  <%if !c.rating && @user === @cuser%>
    <%= form_for c, :url => user_clipping_path(c.user_id, c.id) do |f| %>
      <%= f.label :rating %>
      <%= f.number_field :rating, in: 1...10 %>
      <%= f.submit %>
      <br><br>
    <% end %>
  <% else %>
    <p>Your Rating: <%= c.rating %></p>
    <br>
  <% end %>
<% end %>
<hr>
<% if @user === @cuser %>
  <div = "new-clipping">
    <p>Got room in your garden for a little more?</p>
    <p>Clip a new plant:</p>
    <%=form_for :clipping, :url => user_clippings_path, :action => "post" do |c|%>
      <%= c.collection_select :plant_name, Plant.all, :id, :name %><br><br>
      <%= c.submit "Add This Clipping"%>
    <% end %>
  </div>
<% end %>
