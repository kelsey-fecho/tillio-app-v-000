<ul id="gardensList">
<% @gardens.each do |garden|%>
  <li>
    <%= garden.name %>
  </li>
<% end %>
</ul>

<%= form_for(Garden.new) do |g|%>
  <%= g.text_field :name %><br>
  <%= g.submit %>
<% end %>
