<h3>Hey, <%=@user.name %>!</h3>

<h5>Your Clippings</h5>
<ul>
  <% @user.clippings.each do |c| %>
    <li><%=link_to c.plant.name, plant_path(c.plant) %> - Your Rating: <%= c.rating %></li>
  <% end %>
</ul>

<%= link_to "Browse Plants", plants_path %>
